# Slides (instructor & classroom)

*Authoritative copy: part of the MyST project (`myst.yml`).*

Slide decks live here as structured Markdown. Separators `+++` delimit **frames** for Beamer-style export; the same files are included in the public site build.

Add new decks to `project.toc` in `myst.yml` under **Slides**.
